# Copy this file to `.env` and fill in real values. `.env` is gitignored.
#
#   cp .env.example .env
#
# These credentials are used by the provisioner to create (first run) and
# then log in to the Uptime Kuma admin account.

KUMA_USERNAME=admin
KUMA_PASSWORD=change-me-to-something-strong
